name: Build MOD X Beauty Filters

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'ios-beauty/AladBeautyFilters.m'
      - 'ios-beauty/patch_webrtc.py'
      - '.github/workflows/build-beauty.yml'

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-26
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare WebRTC-safe source
        shell: bash
        run: |
          set -euo pipefail
          python3 ios-beauty/patch_webrtc.py \
            ios-beauty/AladBeautyFilters.m \
            ios-beauty/AladBeautyFiltersWebRTC.m
          grep -q 'WebRTC-safe frame hook installed' ios-beauty/AladBeautyFiltersWebRTC.m
          grep -q 'RTCCameraVideoCapturer' ios-beauty/AladBeautyFiltersWebRTC.m
          grep -q 'ALCopyPixelBufferPixels' ios-beauty/AladBeautyFiltersWebRTC.m

      - name: Build arm64 iOS dylib
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p ios-beauty/build
          SDK="$(xcrun --sdk iphoneos --show-sdk-path)"
          xcrun --sdk iphoneos clang \
            -arch arm64 \
            -miphoneos-version-min=14.0 \
            -fobjc-arc \
            -fmodules \
            -dynamiclib \
            -isysroot "$SDK" \
            -framework UIKit \
            -framework AVFoundation \
            -framework CoreImage \
            -framework CoreMedia \
            -framework CoreVideo \
            -framework CoreGraphics \
            -framework ImageIO \
            -framework Vision \
            -framework QuartzCore \
            -Wl,-undefined,dynamic_lookup \
            -Wl,-install_name,@executable_path/AladBeautyFilters.dylib \
            ios-beauty/AladBeautyFiltersWebRTC.m \
            -o ios-beauty/build/AladBeautyFilters.dylib

      - name: Validate dylib
        shell: bash
        run: |
          set -euo pipefail
          file ios-beauty/build/AladBeautyFilters.dylib | tee ios-beauty/build/file.txt
          file ios-beauty/build/AladBeautyFilters.dylib | grep -Eq 'Mach-O 64-bit.*arm64'
          otool -D ios-beauty/build/AladBeautyFilters.dylib | tee ios-beauty/build/install-name.txt
          otool -D ios-beauty/build/AladBeautyFilters.dylib | grep -q '@executable_path/AladBeautyFilters.dylib'
          nm -u ios-beauty/build/AladBeautyFilters.dylib | grep -q 'MSHookMessageEx'
          strings ios-beauty/build/AladBeautyFilters.dylib | grep -q 'WebRTC-safe frame hook installed'
          strings ios-beauty/build/AladBeautyFilters.dylib | grep -q 'RTCCameraVideoCapturer'
          shasum -a 256 ios-beauty/build/AladBeautyFilters.dylib | tee ios-beauty/build/sha256.txt

      - name: Upload verified dylib artifact
        uses: actions/upload-artifact@v4
        with:
          name: AladBeautyFilters-arm64
          path: ios-beauty/build/AladBeautyFilters.dylib
          if-no-files-found: error
          retention-days: 7

      - name: Encode build for repository retrieval
        shell: bash
        run: |
          set -euo pipefail
          gzip -9 -c ios-beauty/build/AladBeautyFilters.dylib | base64 > ios-beauty/build/AladBeautyFilters.dylib.gz.b64
          rm ios-beauty/build/AladBeautyFilters.dylib
          rm -f ios-beauty/AladBeautyFiltersWebRTC.m

      - name: Commit verified build metadata
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add ios-beauty/build
          if git diff --cached --quiet; then
            echo 'Build output unchanged.'
            exit 0
          fi
          git commit -m 'build: compile MOD X beauty filters [skip ci]'
          git push
